package com.casino.roulette.persistence;

import com.casino.persistence.export.Persister;

public class RoulettePersister implements Persister<RoundResult> {

	@Override
	public void persist(RoundResult result) {
		// data conversions
		// calling async service
		// Virtual Thread? fireAsync?
	}

}
